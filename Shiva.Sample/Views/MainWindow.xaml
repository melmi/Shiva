<Window x:Class="Shiva.Sample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="222.369" Width="272.368">
    <StackPanel Width="200" Margin="30">
        <Button Content="Show Person Dialog" Command="{Binding FirePersonDialog}" Margin="5"/>
        <Button Content="Employee To Company" Command="{Binding FireEmployeeToCompany}" Margin="5"/>
        <Button Content="Company To Employee" Command="{Binding FireCompanyToEmployee}" Margin="5"/>
        <Button Content="Company" Command="{Binding FireCompany}" Margin="5"/>
    </StackPanel>
</Window>
